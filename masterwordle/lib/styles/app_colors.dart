import 'package:flutter/material.dart';

class AppColors {
  // No one likes a white background (even if they dont realise it - they dont)
  static const backgroundColor = Color.fromARGB(255, 240, 238, 237);

  // The color
  static const primaryColor = Color.fromARGB(255, 226, 178, 20);

  // Basic Colours
  static const whiteColor = Color.fromARGB(255, 250, 247, 244);
  static const greyColor = Color.fromARGB(255, 114, 105, 91);
  static const blackColor = Color.fromARGB(255, 14, 11, 8);

  // Status Colours
  static const errorColor = Color.fromARGB(255, 206, 58, 38);
  static const warningColor = Color.fromARGB(255, 255, 177, 59);
  static const successColor = Color.fromARGB(255, 98, 170, 40);
}
